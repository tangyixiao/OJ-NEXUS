package com.ojnexus.core.model

/**
 * Coarse knowledge taxonomy for the mastery system. v0.1 only needs the enum for UI structure;
 * the knowledge tree, problem relations and the mastery engine arrive in later phases.
 */
enum class KnowledgeArea {
    DATA_STRUCTURE,
    GRAPH,
    DYNAMIC_PROGRAMMING,
    STRING,
    MATH,
    GEOMETRY,
    GREEDY,
    SEARCH,
    CONSTRUCTION,
    GAME_THEORY,
}
