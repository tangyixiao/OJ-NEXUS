# OJ NEXUS ProGuard/R8 rules.
# Release minification is disabled in the build config for now; rules land together with
# the first release-critical dependencies (Room/Retrofit rules are added by their libraries).
