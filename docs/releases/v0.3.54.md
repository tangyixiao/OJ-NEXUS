# OJ NEXUS v0.3.54 — Submission Control Tower / 提交控制塔

## Highlights

- Added a local `SUBMISSION PULSE` with total, pending, ready, and failed request counts.
- Added `ALL`, `PENDING`, `READY`, and `FAILED` filters that preserve source order and keep
  unknown statuses visible in ALL.
- Added `CLEAR FILTER` for returning to the complete local request list without modifying jobs.
- Kept existing result checks, background queue/retry actions, request metadata, and problem
  workspace navigation intact.
- Added reduced-motion-safe 200ms count and list transitions with localized accessibility labels.

Package identity: `versionName=0.3.54`, `versionCode=54`.

## Verification

- `.\tools\gradlew-local.bat test --no-daemon --console=plain` — `BUILD SUCCESSFUL`.
- `.\tools\gradlew-local.bat assembleDebug --no-daemon --console=plain` — `BUILD SUCCESSFUL`.
- `.\tools\gradlew-local.bat lintDebug --no-daemon --console=plain` — `BUILD SUCCESSFUL`.
- Installed package on `emulator-5554` reports `versionCode=54` and `versionName=0.3.54`.
- APK SHA-256: `A3F9786D4CF83A11E414ECD4A31D4C8C146B68441EB4442172BB992DF5C8836F`.
- Emulator UI evidence: [initial submission center](../../app/build/reports/ojnexus-submissions-v054.png)
  and [PENDING filter](../../app/build/reports/ojnexus-submissions-v054-filtered.png).
- With the installed local database empty, the pulse showed TOTAL/PENDING/READY/FAILED as zero;
  selecting PENDING exposed CLEAR FILTER and preserved the existing global empty message, then
  CLEAR FILTER restored ALL.
- Existing submission navigation unit tests ran in the full suite. No request row was present for
  a runtime OPEN check, so no fake request or live submission was created.
- Logcat check after the interaction pass: `NO_APP_FATAL_EXCEPTION`.

## Scope

This is a local presentation and list-filtering release. It adds no network fields, database
migration, credentials, passwords, Cookie, Session, CSRF state, cloud service, cross-device sync,
local compiler, custom-input runner, background submission, bulk retry, or automatic POST retry.
/ 这是本地展示与列表筛选版本，不新增网络字段、数据库迁移、凭据、密码、Cookie、Session、CSRF 状态、
云服务、跨设备同步、本地编译器、自定义输入运行器、后台提交、批量重试或自动提交重试。
