# v0.3.58 — Package Identity Refresh

## Highlights

- Advanced the Android package identity from `versionCode=57` / `versionName=0.3.57` to
  `versionCode=58` / `versionName=0.3.58`.
- Kept the Phase 61 Submission Ops Deck behavior and local-only scope unchanged.

## Verification

- `.\tools\gradlew-local.bat test assembleDebug lintDebug --no-daemon --console=plain` — verified after the version bump.
- Installed on `emulator-5554`; Android reports `versionCode=58` and `versionName=0.3.58`.
- APK SHA-256: `BAACC8D2AEE4B363E05E6E6AD401FC9AAAA23DDD9B374908E075525FC3696245`.
