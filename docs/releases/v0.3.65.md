# v0.3.65 — Command Palette Direct Jump

## Highlights

- Added deterministic direct local problem queries to COMMANDS: `cf`, `codeforces`, `ac`,
  `atcoder`, `lg`, `luogu`, and `search` prefixes are supported.
- Added the blue-rail `DIRECT QUERY` result with visible target text and button semantics.
- Direct queries open the existing Problems library and prefill its judge and text filter once;
  ordinary commands and empty/error behavior remain unchanged.

## Scope

This is a local-only presentation, parsing, and navigation change. It adds no network request,
remote catalog access, database migration, credential storage, compiler, or persisted search
state. The existing Problems filter remains the source of truth.

## Package identity

- `versionName=0.3.65`
- `versionCode=65`
- APK SHA-256: `5CA40E5ACAB8F46D2C635E85FE79256F40BD054E50217618FF3A0DA1FF1CBAAD`

## Verification

- `tools/gradlew-local.bat test assembleDebug lintDebug --no-daemon --console=plain` —
  `BUILD SUCCESSFUL`.
- `tools/gradlew-local.bat :app:connectedDebugAndroidTest --no-daemon --console=plain` —
  `BUILD SUCCESSFUL`; `CommandPaletteComposeTest` passed on Pixel 9 API 35.
- Installed the final APK on `emulator-5554`; Android reported `versionCode=65` and
  `versionName=0.3.65`. After a clean launch, `pidof com.ojnexus` returned `8689` and logcat
  contained no `FATAL EXCEPTION`.
- Opened Problems, switched to `REMOTE CATALOG`, then opened `COMMANDS`, entered `cf 1029e`,
  and observed the localized direct-query surface with `DIRECT QUERY`,
  `SEARCH CODEFORCES FOR 1029e`, and `OPEN LOCAL PROBLEM LIBRARY`. Selecting it switched back
  to `LIBRARY` with the `CODEFORCES` filter, query `1029e`, and `1 PROBLEMS` visible.
- Visual evidence: [palette](../../app/build/reports/ojnexus-command-palette-v065-final.png) and
  [filtered library](../../app/build/reports/phase67-v065-remote-result.png).
