# v0.3.55 — Analytics Focus Lens

## Highlights

- Added a local Analytics Pulse for solved, attempts, active days, and training time.
- Added 14D, 30D, and 90D activity-window controls.
- Updated solve-trend and training-time charts from the selected window while preserving the full 365-day heatmap and all-time distributions.
- Added reduced-motion-safe metric and section transitions with English and Simplified Chinese resources.

## Verification

- `:app:testDebugUnitTest --tests com.ojnexus.feature.analytics.AnalyticsFocusLensTest` — pending final verification.
- `:app:compileDebugKotlin` — pending final verification.
- `test` / `assembleDebug` / `lintDebug` — pending final verification.
- Runtime install, Analytics window interaction, screenshot, APK hash, and fatal-log scan — pending final verification.

## Scope

This release is local-only. It adds no network fields, database migration, credentials, cloud
service, cross-device sync, local compiler, custom-input runner, background submission, or
automatic POST retry.
