# v0.3.56 — Workspace Execution Cockpit

## Highlights

- Added a local Workspace Pulse for mode, language, source-code lines, and draft state.
- Exposed O2 optimization for both custom-input runs and problem submissions.
- Carried the first Luogu problem-detail sample pair into Workspace when available.
- Added sample input load/clear actions and a read-only expected-output block.
- Added explicit textual IDLE, PENDING, and READY result state feedback with reduced-motion-safe transitions.

## Verification

Final verification evidence is recorded after the complete test, build, lint, install, and runtime
interaction pass.

## Scope

This release is local-only. It adds no Workspace fetch, network fields, database migration,
credential storage, cloud service, cross-device sync, local compiler, custom-input runner,
background submission, or automatic POST retry.
