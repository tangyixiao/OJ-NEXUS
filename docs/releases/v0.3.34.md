# OJ NEXUS v0.3.34 — Release identity / 发布版本身份

## Highlights / 重点

- The APK now reports `versionName=0.3.34` and `versionCode=34`. / APK 现在报告
  `versionName=0.3.34` 和 `versionCode=34`。
- Android package metadata, the Git tag, and this GitHub Release share one identity. / Android
  包元数据、Git 标签和本 GitHub Release 使用统一身份。
- Earlier releases and release notes remain preserved. / 更早的 Release 和发布说明继续保留。

## Scope / 范围

This release changes release metadata and documentation only. It adds no Luogu endpoint,
credential flow, database migration, cloud service, cross-device sync, local compiler,
custom-input runner, or automatic submission retry. / 本版本仅修改发布元数据和文档，不新增洛谷接口、
凭据流程、数据库迁移、云端服务、跨设备同步、本地编译器、自定义输入运行器或自动提交重试。

## Verification / 验证

```text
git diff --check
.\tools\gradlew-local.bat clean test assembleDebug lintDebug --no-daemon --rerun-tasks --console=plain
```

The full project gate passed. The APK was installed on `emulator-5554`, Android reported
`versionCode=34` and `versionName=0.3.34`, and the app launched successfully. The emulator
remains online. / 项目全量门禁通过。APK 已安装到 `emulator-5554`，Android 报告
`versionCode=34` 和 `versionName=0.3.34`，应用成功启动。模拟器保持在线。

## Artifact / 构建产物

- APK: `app-debug.apk`
- SHA-256: _filled after the final verified build / 最终验证构建后填写_

Earlier release notes and published releases are preserved. / 更早的发布说明和已发布 Release 均保留。
