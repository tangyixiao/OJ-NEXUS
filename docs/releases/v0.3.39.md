# OJ NEXUS v0.3.39 — Luogu settings focus correction / 洛谷设置定位修正

## Highlights / 重点

- The Dashboard `CONNECT LUOGU` route now scrolls Settings to the Luogu public-account panel. / Dashboard 的“连接洛谷”入口现在会将设置页滚动到洛谷公开账号面板。
- Existing OpenApp focus and ordinary Settings navigation remain unchanged. / 原有 OpenApp 聚焦和普通设置导航保持不变。
- Package identity is `versionName=0.3.39`, `versionCode=39`. / 安装包身份为 `versionName=0.3.39`、`versionCode=39`。
- `SHA256SUMS-v0.3.39.txt` accompanies the APK. / APK 附带 `SHA256SUMS-v0.3.39.txt`。

## Safety boundary / 安全边界

This phase fixes local first-use navigation only. It does not add a new network request,
main-site password, Cookie, Session, CSRF state, cloud account, cross-device sync, local
compiler, custom-input runner, or automatic POST retry. / 本阶段仅修正本地首次使用导航，不新增网络请求、
主站密码、Cookie、Session、CSRF 状态、云端账号、跨设备同步、本地编译器、自定义输入运行器或自动 POST 重试。

## Verification / 验证

```text
git diff --check
.\tools\gradlew-local.bat clean test assembleDebug lintDebug assembleRelease --no-daemon --rerun-tasks --console=plain
```

The focused predicate test and route wiring cover the Dashboard Luogu first-use path. The
Release APK is installed over the existing app without clearing data, with package version
`0.3.39`; the emulator remains online. / 聚焦谓词测试和路由连线覆盖 Dashboard 的洛谷首次使用路径。
Release APK 在不清除数据的情况下覆盖安装，包版本为 `0.3.39`；模拟器保持在线。

APK SHA-256: `1531B19B13427F8FD26ABA7C3D516A9E9964C214BE63C726AC4DA320B231F4F6` / APK SHA-256：`1531B19B13427F8FD26ABA7C3D516A9E9964C214BE63C726AC4DA320B231F4F6`。

Earlier release notes and published releases are preserved. / 更早的发布说明和已发布 Release 均保留。
