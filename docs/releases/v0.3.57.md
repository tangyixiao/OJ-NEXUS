# v0.3.57 — Submission Ops Deck

## Highlights

- Added manual `CHECK PENDING` and `QUEUE FAILED` operations to the local Submission Center.
- Collapsed recent-request rows into a compact inspector layout while retaining valid actions.
- Added localized `DETAILS` / `HIDE DETAILS` expansion for request, compile, judge, output, and resource metadata.
- Added reduced-motion-safe expansion and explicit accessibility state descriptions.

## Scope

This release is local-only. Bulk actions reuse existing foreground result checks and the manual
recovery scheduler. It adds no new jobs, network fields, database migration, credential storage,
cloud service, cross-device sync, local compiler, custom-input runner, background submission, or
automatic POST retry.

## Verification

- `.\tools\gradlew-local.bat test assembleDebug lintDebug --no-daemon --console=plain` — `BUILD SUCCESSFUL`.
- Installed on `emulator-5554`; Android reports `versionCode=57` and `versionName=0.3.57`.
- Runtime Submission Center empty state verified with all four counters at `0`; bulk actions and
  detail controls are correctly hidden when no local request exists.
- Bulk request selection, foreground checking, recovery scheduling, expansion labels, and mirrored
  localization are covered by the focused and full JVM test suite.
- Post-launch logcat check: `NO_APP_FATAL_EXCEPTION`.
- APK SHA-256: `AA81F3FDBF9D3112DB6AADD21B591427CE8D4CE68AEBEFC5A77FBFBFC4AF987E`.
- Runtime capture: `app/build/reports/ojnexus-submissions-v057.png`.
