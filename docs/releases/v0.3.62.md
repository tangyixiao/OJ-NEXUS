# v0.3.62 — Session Review Actions

## Scope

- Added `SCHEDULE ATTENTION` to the terminal session debrief.
- The action queues ATTENTION and PENDING problems that are not already in review.
- Batch insertion validates all IDs and writes stage-0 review rows atomically; existing review
  ladders are preserved.
- Room-backed debrief rows reactively change from `OPEN` to `OPEN REVIEW`; when no candidates
  remain, the panel shows `REVIEW QUEUE READY`.
- No network, migration, credentials, background work, compiler, or new session state was added.

## Package identity

- `versionName=0.3.62`
- `versionCode=62`
- APK SHA-256: `3EC00FA174289828316D9F341FDBF8C529813526710E3DAA423446BCDFD73208`

## Verification

- `tools/gradlew-local.bat test assembleDebug lintDebug --no-daemon --console=plain` —
  `BUILD SUCCESSFUL`.
- Installed `app-debug.apk` on `emulator-5554`; Android reported `versionCode=62` and
  `versionName=0.3.62`.
- With an existing finished local session, the debrief initially exposed
  `1 ITEMS READY FOR REVIEW` and `SCHEDULE ATTENTION`. After tapping the action, it showed
  `REVIEW QUEUE READY` and the affected P4551 row changed to `OPEN REVIEW`; existing rows kept
  their review destination.
- The runtime process remained alive (`pidof com.ojnexus` returned `17956`) and the captured
  logcat contained `NO_APP_FATAL_EXCEPTION`.
- Visual evidence: `app/build/reports/ojnexus-session-review-action-v062.png`.
