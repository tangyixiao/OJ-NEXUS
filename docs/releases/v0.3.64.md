# v0.3.64 — Focus Sprint

## Scope

- Added a local `FOCUS SPRINT` plan to Training's inactive session section.
- Due reviews are selected first by due day, due timestamp, and problem ID; ranked targets fill
  the remaining slots, with duplicates removed and a five-problem cap.
- `BUILD SPRINT` opens the existing session form pre-filled with `FOCUS`, 25 minutes, a localized
  sprint tag, and the selected problem IDs; every value remains editable before confirmation.
- Added the plan rail, source tags, metrics, reduced-motion-safe transition, and action semantics.
- Existing session creation, Room transaction, active-session guard, and routes remain unchanged.
- No network, schema migration, background work, compiler, credentials, or new session state was
  added.

## Package identity

- `versionName=0.3.64`
- `versionCode=64`
- APK SHA-256: `A40CEB8FC3653EC9A202F41695835CBC758F37C921F4FE7D3215D7AAABE61E53`

## Verification

- `tools/gradlew-local.bat test assembleDebug lintDebug --no-daemon --console=plain` —
  `BUILD SUCCESSFUL`.
- Installed `app-debug.apk` on `emulator-5554`; Android reported `versionCode=64` and
  `versionName=0.3.64`.
- Training showed `FOCUS SPRINT` with `TARGETS 3 / DUE 0 / RANKED 3`; `BUILD SPRINT` opened the
  session form with `FOCUS`, `25`, `FOCUS SPRINT`, and `3 SELECTED`. The runtime process remained
  alive (`pidof com.ojnexus` returned `22827`) and a clean post-launch logcat reported
  `NO_APP_FATAL_EXCEPTION`.
- Visual evidence: `app/build/reports/ojnexus-focus-sprint-v064.png`.
