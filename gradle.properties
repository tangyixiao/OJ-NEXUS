# OJ NEXUS Gradle properties.
# Machine-specific JDK pinning lives in the USER-level gradle properties
# (~/.gradle/gradle.properties), NOT here — this file must stay CI-safe (Linux).
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
